# Core: Web Service oficial del SAT (e-firma)
requests>=2.31.0
cryptography>=41.0.0
lxml>=4.9.0
urllib3>=2.0.0

# Servidor FastAPI (agente local en localhost:8787)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Portal CIEC (scraping — descarga inmediata para vol. pequeños)
# Instalar además: playwright install chromium
playwright>=1.40.0
