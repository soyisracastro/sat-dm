# Credenciales / e-firma (NUNCA subir)
efirma/
*.cer
*.key
fiel.txt
password.txt

# Excepcion: fixtures de test (certificados fake, no reales)
!tests/fixtures/test_fiel.cer
!tests/fixtures/test_fiel.key
!tests/fixtures/test_fiel.txt

# CFDIs descargados
cfdi_*/
constancia*/
opinion*/
descargas/
# Solo el directorio raíz (salida CSV de la CLI); NO `ui/src/.../metadata/` ni similares.
/metadata/

# Boceto de Claude Design (referencia local; no se versiona — el prototipo se
# re-crea en ui/, no se copia tal cual)
desktop/design-ref/
*.zip
*.csv

# Estado temporal de scripts
descarga_*_state.json

# Diagnósticos HTML (dumps del portal SAT)
diag_*.html
diagnostico_consulta.html
login_page.html

# Scripts de prueba/debug (se quedan locales)
prueba.py
consulta_metadata_2025.py
descarga_2025.py
debug_verificacion.py
diagnostico_ciec.py
diagnostico_constancia.py
inspeccionar_login.py

# Notas internas de desarrollo
CLAUDE.md

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
# Anclados a root: solo /dist y /build de la raíz (pip wheel/sdist), NO los
# build/ de subproyectos como desktop/build/ (que contiene icon.ico versionado).
/dist/
/build/
*.egg

# UI
ui/node_modules/
ui/.next/
ui/out/
ui/next-env.d.ts
ui/tsconfig.tsbuildinfo

# PyInstaller (agente Windows empaquetado)
packaging/dist/
packaging/build/
packaging/__pycache__/

# electron-builder (instaladores y artefactos de salida)
desktop/dist/
desktop/node_modules/

# Logs de electron-log / auto-update (cuando se agreguen)
*.log

# Virtual environment
.venv/
venv/
.env
# Env locales (Next.js convention)
.env.local
.env.*.local
ui/.env.local
ui/.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Salida de diagnóstico CIEC / CSF (screenshots/html/reporte, datos sensibles)
diag_out/
diag_csf/
