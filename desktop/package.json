{
  "name": "todoconta-desktop",
  "productName": "TodoConta",
  "version": "1.3.0",
  "private": true,
  "description": "TodoConta Desktop — Administración de CFDIs y herramientas fiscales",
  "main": "main.js",
  "scripts": {
    "dev": "electron .",
    "debug:packaged": "pnpm --dir ../ui build && env -u ELECTRON_RUN_AS_NODE SAT_RENDERER_URL=app://-/ SAT_RENDERER_BUNDLE_DIR=\"$PWD/../ui/out\" SAT_DEBUG_RENDERER=1 electron .",
    "build:agent": "cd .. && pyinstaller packaging/sat-agent.spec --noconfirm --clean --distpath packaging/dist --workpath packaging/build",
    "build:ui": "cd ../ui && pnpm install --frozen-lockfile && pnpm build",
    "build:installer": "electron-builder",
    "build": "pnpm build:agent && pnpm build:ui && pnpm build:installer"
  },
  "dependencies": {
    "@sentry/electron": "^7.13.0",
    "electron-log": "^5.2.0",
    "electron-updater": "^6.3.9"
  },
  "devDependencies": {
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8"
  },
  "pnpm": {
    "//": "pnpm bloquea postinstall por seguridad; autorizamos solo los que requieren binarios.",
    "onlyBuiltDependencies": [
      "electron",
      "electron-builder",
      "esbuild"
    ]
  }
}
